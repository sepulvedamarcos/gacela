' Gambas class file

'
'
' Business logic handler class
'
'
Private $cPhotoService As PhotoService
Private $Log4Gambas As Log4Gambas3
Private $Titulo As String = "[FImportDialog] "
Private $aFilesDrop As String[]
Private $sTextCheckFalse As String = ("Import photos into an album")
Private $sTextCheckTrue As String = ("Import photos into the album")
Private $iIdAlbum As Integer '' Selected album id

' ==================================================
'
'          Public methods
'
' ==================================================
Public Sub _new(cPhotoService As PhotoService, oLogger As Log4Gambas3, aFilesDrop As String[])
  $cPhotoService = cPhotoService
  $Log4Gambas = oLogger
  $aFilesDrop = aFilesDrop
  
  $Log4Gambas.Info($Titulo & ("Creating dialog window and album list"))
  
  ListEditorPhotos.ReadOnly = True
  ListEditorPhotos.List = aFilesDrop
  ListEditorPhotos.Unique = True
  
  PanelAssociateAlbum.Visible = False
  PanelAssociateAlbum.Y = 4
  PanelAssociateAlbum.X = 182
  ListEditorAvailabeAlbums.ReadOnly = True
  ListEditorAvailabeAlbums.Wrap = True
  ListEditorAvailabeAlbums.Unique = True
  
  CheckBoxEnableAlbum.Text = $sTextCheckFalse
  
  FindAlbums()
  
End

Public Sub Run() As Boolean
  
  $Log4Gambas.Debug($Titulo & "Run method")
  Return Not Me.ShowModal()
  
End

Public Sub BtnOK_Click()
  
  Dim iResp As Integer
  Dim aAl As AlbumEntity[]
  Dim iId As Integer
  Dim aPhotoSummary As String[]
  
  If ListEditorPhotos.List.Count = 0 Then
    Message.Warning(("You must select photos to process"))
    Return
  Else 
    iResp = Message.Question(("Are you sure you want to import the selected photos into the album with the chosen options?"), ("Yes"), ("No"))
    
    If iResp = 2 Then
      Return
      
    Else
      $Log4Gambas.Info($Titulo & ("Starting photo import processing"))
      aPhotoSummary = $cPhotoService.ImportPhotos(ListEditorPhotos.List, Null, Null, $iIdAlbum, CheckBoxDeletePhoto.Value)
      If aPhotoSummary.Count > 0 Then
        Message.Info(("Process finished. To see the summary, click the status bar."))
        
      Endif
    Endif
  Endif
  
  Me.Close()
  
Catch
  $Log4Gambas.Error(("A problem occurred while importing the images") & " [" & Error.Text & "]")
  Message.Error(("A problem occurred while importing the images") & " [" & Error.Text & "]")
  
End

Public Sub BtnCancel_Click()
  
  Me.Close
  
End

Public Sub ButtonBoxFilterFilter()
  
  $Log4Gambas.Debug($Titulo & "Filter escribe [" & ButtonBoxFilter.Text & "]")
  TextAreaAlbumDescription.Text = ""
  FindAlbums(ButtonBoxFilter.Text)
  
End

Public Sub ButtonBoxFilter_Click()
  
  $Log4Gambas.Debug($Titulo & "Filter click with text [" & ButtonBoxFilter.Text & "]")
  TextAreaAlbumDescription.Text = ""
  FindAlbums(ButtonBoxFilter.Text)
  
End

Public Sub CheckBoxEnableAlbum_Click()
  
  $Log4Gambas.Debug($Titulo & ("Checkbox click to associate album ") & "[" & Str(CheckBoxEnableAlbum.Value) & "]")
  If CheckBoxEnableAlbum.Value Then
    PanelList.Enabled = False
    VBoxButtons.Enabled = False
    PanelAssociateAlbum.Visible = True
    CheckBoxEnableAlbum.Text = $sTextCheckFalse
    
  Else
    PanelAssociateAlbum.Visible = False
    PanelList.Enabled = True
    VBoxButtons.Enabled = True
    CheckBoxEnableAlbum.Text = $sTextCheckTrue
    
  Endif
  
End

Public Sub BtnOkAlbum_Click()
  
  Dim sItem As String
  Dim iCuenta As Integer
  
  If TextBoxAlbum.Text = "" Or ListEditorAvailabeAlbums.List.Count = 0 Then
    Message.Warning(("You must select an album to continue"))
    ListEditorAvailabeAlbums.SetFocus()
    Return
  Else
    If TextBoxAlbum.Text <> "" Then 
      For Each sItem In ListEditorAvailabeAlbums.List
        If sItem = TextBoxAlbum.Text Then 
          iCuenta += 1
        Endif
      Next
      If iCuenta = 0 Then
        ListEditorAvailabeAlbumsAdd()
      Endif
    Endif
  Endif
  
  PanelAssociateAlbum.Visible = False
  PanelList.Enabled = True
  VBoxButtons.Enabled = True
  
  $Log4Gambas.Debug($Titulo & ("Select an album to associate") & " [" & TextBoxAlbum.Text & "]")
  CheckBoxEnableAlbum.Text = $sTextCheckTrue & " [" & TextBoxAlbum.Text & "]"
  LabelAlbumDescriptionSelected.Text = TextAreaAlbumDescription.Text
  
End

Public Sub BtnCancelAlbum_Click()
  
  $Log4Gambas.Debug($Titulo & ("Cancel album association, revert changes"))
  CheckBoxEnableAlbum.Value = False
  CheckBoxEnableAlbum.Text = $sTextCheckFalse
  
End

Public Sub ListEditorAvailabeAlbumsAdd()
  
  Dim cAlbum As AlbumEntity
  Dim aAlbums As AlbumEntity[]
  
  $Log4Gambas.Debug($Titulo & ("Attempting to add a new album"))
  If TextBoxAlbum.Text = "" Then 
    Message.Warning(("You must enter an album name to continue"))
    TextBoxAlbum.SetFocus()
    Return 
  Endif
  
  aAlbums = ListEditorAvailabeAlbums.Tag
  For Each cAlbum In aAlbums
    If cAlbum.Name = TextBoxAlbum.Text Then 
      Return 
    Endif
  Next
  
  $Log4Gambas.Debug($Titulo & ("New album: " & "{" & TextBoxAlbum.Text & "}{" & TextAreaAlbumDescription.Text & "}"))
  If $cPhotoService.AddAlbum(TextBoxAlbum.Text, TextAreaAlbumDescription.Text) Then
    $Log4Gambas.Debug($Titulo & ("New album added successfully, loading it into the list"))
    FindAlbums()
    
  Else
    $Log4Gambas.Error($Titulo & ("Failed to add new album"))
    Message.Error(("Error while adding a new album"))
    
  Endif
  
Catch
  $Log4Gambas.Error($Titulo & ("Problems creating an album"))
  Message.Error(("Problems creating an album"))
  
End

Public Sub ListEditorPhotosAdd()
  
  Dim vImg As Variant
  
  Dialog.Title = "Select a file"
  Dialog.Filter = ["*.png; *.jpg", "Image files"]
  Dialog.FilterIndex = 0
  Dialog.Path = Desktop.GetDirectory("PICTURES")
  If Dialog.OpenFile(True) Then
    Return ' Process canceled by the user
  Endif
  
  $Log4Gambas.Debug($Titulo & ("Adding new photos"))
  
  For Each vImg In Dialog.Paths
    $Log4Gambas.Debug($Titulo & ("Adding a new photo") & "[" & vImg & "]")
    ListEditorPhotos.Add(vImg)
  Next
  
End

Public Sub ListEditorPhotosSelect()
  
  PictureBoxPreview.Image = Image.Load(ListEditorPhotos.Text)
  
End

Public Sub ListEditorAvailabeAlbumsSelect()
  
  Dim aAlbums As AlbumEntity[]
  Dim cAlb As AlbumEntity
  
  If ListEditorAvailabeAlbums.Index = -1 Then
    $Log4Gambas.Debug($Titulo & ("Detected deletion of an album") & "[" & ListEditorAvailabeAlbums.Text & "]")
    
    aAlbums = ListEditorAvailabeAlbums.Tag
    For Each cAlb In aAlbums
      If cAlb.name = ListEditorAvailabeAlbums.Text Then 
        $Log4Gambas.Debug($Titulo & ("Deletion data matches, sending id") & " [" & cAlb.Id & "] [" & ListEditorAvailabeAlbums.Text & "]")
        If $cPhotoService.RemoveAlbum(cAlb.Id) Then
          TextBoxAlbum.Text = ""
          TextAreaAlbumDescription.Text = ""
          FindAlbums()
        Endif
      Endif
    Next
    
    Return
  Endif
  
  aAlbums = ListEditorAvailabeAlbums.Tag
  
  TextBoxAlbum.Text = aAlbums[ListEditorAvailabeAlbums.Index].Name
  TextAreaAlbumDescription.Text = aAlbums[ListEditorAvailabeAlbums.Index].Description
  $iIdAlbum = aAlbums[ListEditorAvailabeAlbums.Index].Id
  
  $Log4Gambas.Debug($Titulo & ("Album selection") & "[" & TextBoxAlbum.Text & "][" & Str($iIdAlbum) & "]")
  
End

' ==================================================
'
'          Private methods
'
' ==================================================
Private Sub FindAlbums(Optional text As String)
  
  Dim aAlbums As AlbumEntity[]
  Dim cAlbum As AlbumEntity
  
  $Log4Gambas.Debug($Titulo & "Searching for album [" & text & "]")
  aAlbums = $cPhotoService.FindAlbums(text, 0, Constants.USE_SOFT_FALSE)
  
  ListEditorAvailabeAlbums.Clear
  ListEditorAvailabeAlbums.Index = -1
  ListEditorAvailabeAlbums.Tag = aAlbums
  For Each cAlbum In aAlbums
    
    ListEditorAvailabeAlbums.Add(cAlbum.Name)
    
  Next
  
End
